{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jbeshir/demesne/docs/reference/tools/sandbox_wait.schema.json",
  "title": "sandbox_wait input",
  "type": "object",
  "properties": {
    "job_id": {
      "type": "string",
      "description": "Job ID returned by a background sandbox_script, sandbox_agent, or sandbox_research call."
    },
    "timeout_seconds": {
      "type": "number",
      "description": "Maximum seconds to wait for the job to reach a terminal state. 0 or omitted → 1800 s (30 minute) default; hard-capped at 172800 s (48 hours).",
      "default": 1800
    }
  },
  "required": ["job_id"],
  "additionalProperties": false
}
