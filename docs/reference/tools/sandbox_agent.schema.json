{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jbeshir/demesne/docs/reference/tools/sandbox_agent.schema.json",
  "title": "sandbox_agent input",
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "Task for the agent. Free-form text."
    },
    "agent": {
      "type": "string",
      "description": "Agent provider. 'codex' or 'claude-code' — defaults to 'codex' when Codex credentials are configured, otherwise 'claude-code'. The live MCP input schema constrains this to an enum of the providers whose credentials are configured (omitted when neither is)."
    },
    "model": {
      "type": "string",
      "description": "Model for the agent. Provider-specific: claude-code uses 'opus', 'sonnet' (default), or 'haiku'; codex uses 'gpt-5.5' (default) or 'gpt-5.4-mini'. The live MCP input schema constrains this to the union of the configured providers' models."
    },
    "preamble": {
      "type": "string",
      "description": "Optional prose prepended verbatim to the generated agent context file (e.g. CLAUDE.md for claude-code) before the auto-generated environment section."
    },
    "egress": {
      "type": "string",
      "description": "Additional outbound network policy. 'none' (default) means only the vendor proxy; 'package-managers' also allows npm/PyPI/conda registries. 'open' is rejected.",
      "enum": ["none", "package-managers"],
      "default": "none"
    },
    "files": {
      "type": "array",
      "description": "Host file paths to mount read-only into /in/<basename>. Each path must be absolute and inside DEMESNE_ALLOWED_PATHS.",
      "items": {
        "type": "string"
      }
    },
    "directories": {
      "type": "array",
      "description": "Host directory paths to mount read-only into /in/<basename>. Each path must be absolute and inside DEMESNE_ALLOWED_PATHS.",
      "items": {
        "type": "string"
      }
    },
    "output_path": {
      "type": "string",
      "description": "Optional. Where the agent should write its final artefact (e.g. /out/summary.md)."
    },
    "output_format": {
      "type": "string",
      "description": "Optional. Expected shape/format of the output."
    },
    "success_criteria": {
      "type": "array",
      "description": "Optional. Checklist of conditions the output must satisfy.",
      "items": { "type": "string" }
    }
  },
  "required": ["prompt"],
  "additionalProperties": false
}
